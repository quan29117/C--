#include <iostream>     
#include <fstream>
#include <sstream>
#include <string>
#include <vector>
#include <conio.h>
int main () {
         
        // std::vector <int> *A = new std::vector <int>;
        // std::vector <int>::iterator it;
        // A->assign(5,30);
        // for (it =A->begin(); it!=A->end();it++)
        //         std::cout<<(*it)<<" ";
        
        std::cout<<char (getch());
        return 0;
        
}